<resources>

  <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>
  </style>

  <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
    <item name="android:windowBackground">@drawable/splash_background</item>
  </style>

  <!-- Top Ten Songs Screen -->
  <style name="TopTenSongs.Theme" parent="Theme.AppCompat.Light.NoActionBar">
  </style>

  <style name="TopTenSongs.Toolbar" parent="Widget.AppCompat.Toolbar">
    <item name="contentInsetStart">12dp</item>
    <item name="android:textSize">22sp</item>
    <item name="android:textStyle">bold</item>
  </style>

  <style name="TopTenSongs.CollapsingToolbar" parent="">
    <item name="collapsedTitleTextAppearance">@style/TopTenSongs.Logo</item>
    <item name="contentScrim">@color/colorPrimary</item>
    <item name="expandedTitleTextAppearance">@style/TopTenSongs.Logo</item>
    <item name="android:textSize">18sp</item>
    <item name="android:textColor">@android:color/white</item>
  </style>

  <style name="TopTenSongs.Logo" parent="TextAppearance.AppCompat.Display3">
    <item name="android:textAllCaps">true</item>
    <item name="android:textSize">22sp</item>
    <item name="android:textStyle">bold</item>
    <item name="android:textColor">@android:color/white</item>
  </style>

</resources>
